package algs.implementation;

import java.util.HashSet;
import java.util.Set;

/**
 * @author Sergiy Levchynskyi
 */

public class LisasWorkbook {

/*    static int workbook(int n, int k, int[] arr) {
        int count = 0;
        int page = 1;
        Set<Integer> problems = new HashSet<>();
        for (int i = 1; i <= arr.length; i++) {
            int pagesPerChapter = 1;
            pagesPerChapter += arr[i] / k;
            pagesPerChapter += arr[i] % k > 0 ? 1 : 0;
            int endChapterPage =
            int startProblem = 1;
            int endProblem = pagesPerChapter > 1 ? k : arr[i];
            for (int j = page; page <= j + pagesPerChapter; page++) {
                if (page >= startProblem && page <= endProblem) {
                    count++;
                    startProblem = endProblem + 1;
                }
            }
            while (page)
            if (arr[i] >= k) {
                page <
            }
        }

    }*/


    public static void main(String[] args) {
        System.out.println("alfresco".equals("alfresco"));
        System.out.println("admin".equals("admin"));

    }
}
